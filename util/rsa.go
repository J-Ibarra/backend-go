package util

// openssl genrsa -out app_rsa.pem 512
// openssl rsa -in app_rsa.pem -out app_rsa.pub -pubout

// openssl base64 -in app_rsa.pem | tr -d '\n'
// openssl base64 -in app_rsa.pub | tr -d '\n'
